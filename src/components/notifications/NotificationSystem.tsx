import { useEffect } from 'react';
import { useSupabaseData } from '@/hooks/useSupabaseData';
import { useToast } from '@/hooks/use-toast';

export function NotificationSystem() {
  // Notifications are disabled as requested by user
  return null;
}